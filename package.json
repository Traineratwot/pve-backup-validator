{
  "name": "pve-backup-validator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "install:cron": "bun run src/install.ts",
    "uninstall:cron": "bun run src/uninstall.ts"
  },
  "dependencies": {
    "valibot": "^1.0.0"
  }
}
