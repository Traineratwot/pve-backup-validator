{
  "name": "pve-backup-validator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "setup": "bun run src/install.ts",
    "install:cron": "bun run src/install-cron.ts",
    "uninstall:cron": "bun run src/uninstall.ts"
  },
  "dependencies": {
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}
